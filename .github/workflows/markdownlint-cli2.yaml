name: Build markdownlint-cli2 for Debian

on:
  workflow_dispatch:
    inputs:
      version:
        description: "markdownlint-cli2 release tag"
        required: true
        default: "v0.18.1"

permissions:
  contents: write

jobs:
  build-deb:
    name: Build markdownlint-cli2 Debian package
    runs-on: ubuntu-24.04
    container: debian:sid

    env:
      VERSION: ${{ github.event.inputs.version }}
      DEB_BUILD_OPTIONS: nocheck

    steps:
      - name: Checkout packaging repo
        uses: actions/checkout@v4
        with:
          path: packaging

      - name: Install build dependencies
        run: |
          apt-get update
          apt-get install -y --no-install-recommends \
            ca-certificates \
            curl \
            dpkg-dev \
            devscripts \
            equivs \
            xz-utils
          mk-build-deps -i -t "apt-get -y --no-install-recommends" \
            ./packaging/markdownlint-cli2-deb/debian/control

      - name: Prepare Debian packaging
        run: |
          set -e
          UPSTREAM_VERSION="${VERSION#v}"
          curl -L \
            -o "markdownlint-cli2_${UPSTREAM_VERSION}.orig.tar.gz" \
            "https://github.com/DavidAnson/markdownlint-cli2/archive/refs/tags/v${UPSTREAM_VERSION}.tar.gz"
          tar -xzf "markdownlint-cli2_${UPSTREAM_VERSION}.orig.tar.gz"
          cd "markdownlint-cli2-${UPSTREAM_VERSION}"

          cp -a ../packaging/markdownlint-cli2-deb/debian ./debian
          chmod +x debian/rules
          sed \
            -e "s/@UPSTREAM_VERSION@/${UPSTREAM_VERSION}/" \
            -e "s/@DATE@/$(date -R)/" \
            debian/changelog.in > debian/changelog
          rm -f debian/changelog.in

      - name: Build Debian package
        run: |
          UPSTREAM_VERSION="${VERSION#v}"
          cd "markdownlint-cli2-${UPSTREAM_VERSION}"
          dpkg-buildpackage -us -uc -b
          rm -f ../*.buildinfo ../*.changes

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: markdownlint-cli2-deb
          path: |
            markdownlint-cli2_*_all.deb

      - name: Create or update Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: markdownlint-cli2-${{ env.VERSION }}
          name: markdownlint-cli2 ${{ env.VERSION }}
          draft: false
          prerelease: false
          files: |
            markdownlint-cli2_*_all.deb
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
