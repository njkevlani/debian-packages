#!/usr/bin/make -f
export DH_VERBOSE = 1
export DEB_BUILD_MAINT_OPTIONS = hardening=+all

%:
	dh $@

override_dh_auto_build:
	:

override_dh_auto_install:
	npm install --omit=dev --ignore-scripts --global --prefix "$(CURDIR)/debian/markdownlint-cli2/usr" .
